# Product Context
The landing page must establish the chill vibe immediately. It must be loud, unapologetic, and fast.
**CRITICAL: All UI implementations MUST strictly adhere to the guidelines defined in `.impeccable.md`. Use the specific shadcn/ui components overridden in step 01.**

*   **Epic: Landing Page Experience**
    *   **Feature: Hero Banner**
        *   [x] Task: Build the hero section block using grid/block layouts according to the "Feature-Rich Showcase" pattern in `.impeccable.md`. Ask `@frontend-design` for a distinctive, production-grade implementation.
        *   [x] Task: Insert an overwhelmingly large and bold headline using the Space Grotesk typography stack. Use `@clarify` and `@humanizer` to refine the copy.
        *   [x] Task: Implement pop-art illustrations depicting the three herbal blends, applying appropriate `shadow-xl` spacing layers based on `.impeccable.md`. Use `@delight` for unexpected, joyful touches.
    *   **Feature: Value Proposition Block**
        *   [x] Task: Build a pitch section below the hero utilizing strict `.impeccable.md` spacing variables (`--space-2xl` / `--space-3xl`).
        *   [x] Task: Layout 3-4 massive bullet points emphasizing a better vibe, zero anxiety, and pure taste. Use prominent Lucide icons with `strokeWidth={2.5}` or `strokeWidth={3}`. Use `@bolder` to increase impact.
        *   [x] Task: Integrate mandatory compliance terms naturally into the copy structure. Use `@humanizer` to ensure it avoids corporate patterns.
